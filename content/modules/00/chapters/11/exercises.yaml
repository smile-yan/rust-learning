- title: 位运算判断奇偶
  description: 用 & 运算符判断一个整数是奇数还是偶数。
  code_template: |-
    fn main() {
        let n = 7;
        // if n & 1 == 0 { ... }
    }
- title: 复合赋值
  description: 声明 mut total = 10，分别用 +=、*= 改变其值并打印。
  code_template: |-
    fn main() {
        let mut total = 10;
        // total += 5;
        // total *= 2;
        println!("{}", total);
    }
