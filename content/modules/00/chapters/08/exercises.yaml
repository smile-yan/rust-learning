- title: 方向枚举
  description: 定义 Direction { North, South, East, West }，用 match 打印中文方向。
  code_template: |-
    enum Direction {
        North, South, East, West,
    }

    fn show(d: Direction) {
        // match d { ... }
    }

    fn main() {
        show(Direction::East);
    }
- title: 枚举携带消息
  description: 定义 Message::Text(String) 和 Message::Number(i32)，用 match 分别处理。
  code_template: |-
    enum Message {
        Text(String),
        Number(i32),
    }

    fn main() {
        let msg = Message::Text(String::from("hi"));
        // match msg { ... }
    }
