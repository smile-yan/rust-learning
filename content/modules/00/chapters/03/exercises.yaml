- title: 类型转换
  description: 把 i32 类型的 42 显式转换为 f64，与 3.14 相加后打印。
  code_template: |-
    fn main() {
        let a: i32 = 42;
        let b: f64 = 3.14;
        // let c = ...;
        println!("{}", c);
    }
- title: 元组解构
  description: 定义一个 (&str, i32, bool) 元组，用模式匹配解构后分别打印。
  code_template: |-
    fn main() {
        let t: (&str, i32, bool) = ("Rust", 2021, true);
        // let (name, year, ok) = t;
    }
