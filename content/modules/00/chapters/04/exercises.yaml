- title: 实现 multiply
  description: 编写一个 multiply 函数，接收两个 i32 参数并返回乘积。
  code_template: |-
    fn multiply(a: i32, b: i32) -> i32 {
        // 补全函数体
    }

    fn main() {
        println!("{}", multiply(3, 4));
    }
- title: 用可变引用自增
  description: '编写 fn increment(x: &mut i32)，在 main 中调用它让变量加 1。'
  code_template: |-
    fn increment(x: &mut i32) {
        // 补全
    }

    fn main() {
        let mut n = 0;
        increment(&mut n);
        println!("{}", n);
    }
