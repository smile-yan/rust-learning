- title: 最长的字符串切片
  description: 实现 longest 函数并调用。
  code_template: |-
    fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
        if x.len() > y.len() { x } else { y }
    }

    fn main() {
        println!("{}", longest("abc", "de"));
    }
