- title: clone 避免移动
  description: 用 clone 让 s1 和 s2 都能使用。
  code_template: |-
    fn main() {
        let s1 = String::from("Rust");
        let s2 = s1.clone();
        println!("{} {}", s1, s2);
    }
