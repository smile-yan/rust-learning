- title: 返回较长的字符串切片
  description: '实现 longest<''a>(x: &''a str, y: &''a str) -> &''a str，返回较长的那个。'
  code_template: |-
    fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
        // if x.len() > y.len() { x } else { y }
    }

    fn main() {
        let s1 = String::from("abc");
        let s2 = "xy";
        println!("{}", longest(s1.as_str(), s2));
    }
