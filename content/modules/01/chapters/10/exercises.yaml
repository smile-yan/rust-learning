- title: 提取手机号
  description: 用 regex 提取文本中所有形如 1xx-xxxx 的号码。
  code_template: |-
    fn main() {
        let text = "手机: 138-1234, 固话: 010-5678";
        // let re = Regex::new(r"...").unwrap();
    }
- title: 替换敏感词
  description: 将文本中的 "rust" 替换为 "RUST"（不区分大小写）。
  code_template: |-
    fn main() {
        let text = "I love rust and RUST";
        // let re = Regex::new(r"(?i)rust").unwrap();
    }
