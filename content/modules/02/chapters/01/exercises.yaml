- title: 在 unsafe 块中解引用裸指针
  description: 创建一个 *const i32 指向局部变量，并在 unsafe 块中打印它指向的值。
  code_template: |-
    fn main() {
        let x = 42;
        let r = &x as *const i32;
        unsafe {
            println!("{}", *r);
        }
    }
