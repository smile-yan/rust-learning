- title: 声明并调用 C 的 abs
  description: 用 extern "C" 声明 abs，在 unsafe 块中调用。
  code_template: |-
    extern "C" {
        fn abs(input: i32) -> i32;
    }

    fn main() {
        unsafe {
            println!("{}", abs(-10));
        }
    }
